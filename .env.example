# Local development values belong in .env.development.local.
# Production values belong in Cloudflare environment configuration.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Production server secrets must never use PUBLIC_ names.
SERVER_RUNTIME_PROOF_VALUE=
